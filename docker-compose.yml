version: '3.8'
services:
  app:
    build: .
    container_name: lucas
    ports:
      - "8501:8501"
    volumes:
      - .:/app          # Mapea el código (Hot-reload)
      - ./data:/app/data # Persistencia de la DB
    environment:
      - PYTHONUNBUFFERED=1